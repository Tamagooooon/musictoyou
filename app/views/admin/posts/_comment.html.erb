<div class="col-lg-12 comment">
  <table class="table table-hover table-inverse">
    <thead>
      <tr class="text-white">
        <th align="center"><h3><strong>コメント</strong></h3></th>
      </tr>
      </tr>
    </thead>
    <tbody>
      <% post.post_comments.each do |post_comment| %>
      <tr class="text-white">
        <td align="center">
        <p><%= image_tag post_comment.user.get_profile_image(50, 50), class: "rounded-circle" %>　　<strong><%= post_comment.user.name %></strong></p>
          
        <h5><%= post_comment.comment %></h5>
          
        <%= post_comment.created_at.strftime('%Y/%m/%d') %>
          
        <%= link_to "コメント削除", post_post_comment_path(post_comment.post, post_comment), class: "btn btn-outline-danger", 
                                                                      method: :delete, "data-confirm" => "本当にコメントを削除しますか?" %>
      </td>
      <% end %>
    </tbody>
  </table>
</div>