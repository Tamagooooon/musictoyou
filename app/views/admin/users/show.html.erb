<div class="container">
  <div class="row">
    <div class="col-lg-12 text-center text-white">
      <h1><strong>ユーザー詳細</strong></h1>
    </div>

    <div class="col-lg-4 text-center text-white">
      <h3>ユーザー名　<strong><%= @user.name %></strong></h3>

      <%= image_tag @user.get_profile_image(100,100), class: "rounded-circle" %>

      <% if @user.id %>
        <p><%= link_to "プロフィール編集", edit_admin_user_path(@user), class: "btn btn-outline-light" %></p>
      <% end %>

    </div>

    <div class="col-lg-8 text-center">
      <%= render 'admin/posts/list', posts: @posts %>
    </div>
  </div>
</div>